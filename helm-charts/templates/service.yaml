apiVersion: v1
kind: Service
metadata:
  name: {{ include "cert-monitor.fullname" . }}
  labels:
    {{- include "cert-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "cert-monitor.selectorLabels" . | nindent 4 }}
